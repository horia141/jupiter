"""The key for a metric which acts as both name and unique identifier."""
from jupiter.domain.entity_key import EntityKey


class MetricKey(EntityKey):
    """The key for a metric which acts as both name and unique identifier."""
